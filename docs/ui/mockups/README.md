# Duo UI 方案稿

本地预览：`cd /home/luyu/duo && python -m http.server -d docs/ui/mockups 8899`，
浏览器打开 <http://localhost:8899>。

文件清单：`panel.html` 主面板 / `settings.html` 设置页 / `icons.html` 应用图标评审 / `index.html` 索引。
